name: sort_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - scikit-learn
  - tqdm
  - ipython
  - viennarna
  - pip
  - biopython
  - cmap
  - pip:
      - polars
      - flowkit
      - mavenn
      - vcf_parser

